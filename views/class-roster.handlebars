  {{#if loggedIn}}
  {{> navbar}}
    
      <section>
          <h3 class="heading">Class Roster</h3>
          <br>
          <div class="container class-roster-container">
              <div class="row">
                  <p class="lastName col col-name">Last Name</p>
                  <p class="firstName col col-name">First name</p>
                  <p class="guardian col col-name">Guardian</p>
                  <p class="email col col-name">Guardian Email</p>
              </div>
  
            {{#each students as |student| }}
              <div class="row">
                  <p class="lastName col">{{student.last_name}}</p>
                  <p class="firstName col">{{student.first_name}}</p>
                  <p class="guardian col ">{{student.guardian}}</p>
                  <a class="email col guardianEmail"  href="mailto:{{student.guardian_email}}" class="email col">{{student.guardian_email}}</a>
              </div>
            {{/each}}

              <form class="row add-student-form">
                <input type="text" id="lastName" placeholder="Last Name" name="lastName" class="lastName col">
                <input type="text" id="firstName" name="firstName" placeholder="First Name" class="firstName col">
                <input type="text" id="guardian" name="guardian" placeholder="Guardian Name" class="guardian col">
                <input type="email" id="guardianEmail" class="email col" placeholder="email@domain.com" name="email">
            </form>
            <br>
            <button type="submit" class="btn btn-secondary addStudent">Submit</button>

      </section>

      <script src="/javascript/addstudent.js"></script>
      {{/if}}
